version: '3.8'
services:
  infra:
    build: .
    ports:
      - "1452:1452" # API proxy
      - "1453:1453" # 管理介面
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD} # 透過 env 設定 admin 密碼
      - DATABASE_FILE=/data/infra.db
    volumes:
      - infra-data:/data

volumes:
  infra-data:
